<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- MLX compiles Metal shaders at runtime into executable memory. -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- PyInstaller bundles many third-party dylibs not signed with our Team ID. -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- PyInstaller bootloader sets DYLD_* before spawning the Python interpreter. -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
</dict>
</plist>
